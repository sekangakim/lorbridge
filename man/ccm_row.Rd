% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ccm_helpers.R
\name{ccm_row}
\alias{ccm_row}
\title{Closeness-of-Concordance Measures from OR and CI Endpoints}
\usage{
ccm_row(OR, OR_lo, OR_hi, LOR, LOR_lo, LOR_hi)
}
\arguments{
\item{OR}{Numeric. Odds ratio point estimate.}

\item{OR_lo}{Numeric. Lower confidence limit of the odds ratio.}

\item{OR_hi}{Numeric. Upper confidence limit of the odds ratio.}

\item{LOR}{Numeric. Log-odds ratio point estimate.}

\item{LOR_lo}{Numeric. Lower confidence limit of the log-odds ratio.}

\item{LOR_hi}{Numeric. Upper confidence limit of the log-odds ratio.}
}
\value{
A one-row \code{data.frame} with columns:
OR, OR_lo, OR_hi, LOR, LOR_lo, LOR_hi,
YuleQ, Q_lo, Q_hi, YuleY, Y_lo, Y_hi,
r_meta, r_lo, r_hi.
}
\description{
Computes a full row of closeness-of-concordance measures (CCMs) from an
odds ratio (OR), its confidence interval endpoints, and the corresponding
log-odds ratio (LOR). CCMs include Yule's Q, Yule's Y, and the
meta-analytic correlation r_meta (probit transformation of LOR), all on
the [-1, +1] scale introduced by Kim and Grochowalski (2019).
}
\details{
\strong{Yule's Q} = (OR - 1) / (OR + 1). Ranges from -1 to +1; equals the
Pearson correlation for 2x2 tables under a tetrachoric model.

\strong{Yule's Y} = (sqrt(OR) - 1) / (sqrt(OR) + 1). A shrunken version of Q
with better sampling properties for sparse tables.

\strong{r_meta} converts LOR to Cohen's d via d = LOR * sqrt(3) / pi, then to
a correlation-like metric via d / sqrt(d^2 + 4). Equivalent to the
biserial correlation used in meta-analysis.
}
\examples{
lc <- lor_ci_2x2(30, 25, 28, 24)
ccm_row(exp(lc$lor), exp(lc$lo), exp(lc$hi), lc$lor, lc$lo, lc$hi)

}
\references{
Kim, S.-K., & Grochowalski, J. H. (2019). Gaining from discretization of
continuous data: The correspondence analysis biplot approach.
\emph{Behavior Research Methods}, 51(2), 589-601.
\doi{10.3758/s13428-018-1161-1}
}
